spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/spring-cloud-samples/config-repo
          skip-ssl-validation: true
          default-label: main
server:
  port: 8888